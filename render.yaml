services:
  - type: web
    name: traveler-dev
    runtime: node
    buildCommand: npm install
    startCommand: npx tsx gateway/studio.ts
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
