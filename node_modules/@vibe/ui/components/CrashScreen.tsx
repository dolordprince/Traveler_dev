import React from 'react';
export const CrashScreen = ({ error }: { error?: any }) => (
  <div style={{color:'white',background:'#0a0a0a',padding:40,fontFamily:'monospace'}}>
    <h2>Something went wrong</h2>
    <pre>{String(error?.message || error || 'Unknown error')}</pre>
  </div>
);
